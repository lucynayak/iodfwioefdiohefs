# Compiled APKs
*.apk
*.idsig

# Build tools (скачиваются отдельно)
apktool.jar
uber-apk-signer.jar

# Build output
/build/
"horrible build"/

# Binary libs (тяжёлые, не нужны в репо)
lib/
*.so

# Build version counter
.build_version

# Python cache
__pycache__/
*.pyc

# Decoded source copies (генерируются apktool)
horrible_62-aligned-debugSigned_src/
Minecraft PE 1.1.5_src/
